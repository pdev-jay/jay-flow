---
name: plan-deferred-scope
runs: 3
max_turns: 60
timeout_seconds: 900
allowed_tools: [Read, Glob, Grep, Skill, TodoWrite]
---

이 CLI에 항목을 한 번에 여러 개 등록할 수 있게 해줘. 지금은 `python3 cli.py create apple` 처럼 하나씩만 되는데 `python3 cli.py create apple pear grape` 하면 세 개 다 등록되게. 하나라도 업로드가 실패하면 어떻게 할지는 네가 정해서 제안해 줘.

edit 쪽은 아직 덜 된 거 알고 있는데 그건 나중에 해도 돼. 이번엔 create만.

되물을 게 있으면 plan 초안을 먼저 전부 제시하고, 질문은 초안 아래에 권장 답과 함께 붙여줘. 한 번에 보고 싶어.
plan 세우자.
